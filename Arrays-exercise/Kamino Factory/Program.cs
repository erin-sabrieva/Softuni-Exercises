using System;

namespace Kamino_Factory
{
    class Program
    {
        static void Main(string[] args)
        {
            int length = int.Parse(Console.ReadLine());
            string input = "";
            while (input!="Clone them!")
            {
                input = Console.ReadLine(int.Parse());
            }
        }
    }
}
